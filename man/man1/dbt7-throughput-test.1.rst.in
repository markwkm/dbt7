=====================
 dbt7-throughput-test
=====================

---------------
Database Test 7
---------------

:Date: @MANDATE@
:Manual section: 1
:Manual group: Database Test 7 @PROJECT_VERSION@ Documentation
:Version: Database Test 7 @PROJECT_VERSION@

SYNOPSIS
========

**dbt7-throughput-test** [option...]

DESCRIPTION
===========

**dbt7-throughput-test** is the Database Test 7 (DBT-7) throughput tester.
It is normally invoked by **dbt7-run**\ (1), which exports the environment
variables described below.

The throughput test runs two iterations of *streams* concurrent query
streams, each iteration followed by a data maintenance test, per the TPC-DS
specification.  A total of (2 x *streams*) + 1 query stream files are
generated.

OPTIONS
=======

-a DBMS  *dbms* to test, see *dbms* options below.  Required.
-d DIALECT  *dialect* override.
-o PATH  *path* to save results.
-i INPUT  *input* override.
-n STREAMS  Number of *streams* per throughput test iteration.  Required.
-r  Restart database, if already running.
-s SCALE  *scale* factor, default 1.
-S FILE  Seed *file*, default *PATH*/../seed.
--stats  Enable system and database stats collection.
-U  Run test as an unprivileged operating system and database user.  By
        default the test is run as a privileged user that can stop and start
        the database between the individual tests, as allowed per
        specification.
-V, --version  Output version information, then exit.
-Y  Enable profiling (Linux only).
--help  This usage message.  Or **-?**.

PostgreSQL options (pgsql):

-p PARAMETERS  Database *parameters*.

*dbms* options are:

* gp Greenplum
* pgsql PostgreSQL

ENVIRONMENT
===========

The following variables are required and are normally exported by
**dbt7-run**\ (1):

**DBT7DBNAME**

    Name of the database to test.

**DSTOOLS**

    Path to the TPC-DS Tools *tools* subdirectory.

**DSDISTRIBUTIONS**

    Path to the TPC-DS Tools *tpcds.idx* distributions file.

**DS_DATA**

    Directory where the data maintenance refresh files are generated.

SEE ALSO
========

**dbt7**\ (1), **dbt7-run**\ (1), **dbt7-load-test**\ (1),
**dbt7-power-test**\ (1)

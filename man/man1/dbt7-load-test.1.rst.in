===============
 dbt7-load-test
===============

---------------
Database Test 7
---------------

:Date: @MANDATE@
:Manual section: 1
:Manual group: Database Test 7 @PROJECT_VERSION@ Documentation
:Version: Database Test 7 @PROJECT_VERSION@

SYNOPSIS
========

**dbt7-load-test** [option...]

DESCRIPTION
===========

**dbt7-load-test** is the Database Test 7 (DBT-7) load tester.  It is
normally invoked by **dbt7-run**\ (1), which exports the environment
variables described below.

OPTIONS
=======

-a DBMS  *dbms* to test, see *dbms* options below.  Required.
-o PATH  *path* to save results.  Required; created if it does not exist.
--parallel CHUNKS  Parallelize the load into CHUNKS parts, default detected
        number of logical processors.
-s SCALE  *scale* factor, default 1.
--stats  Enable system and database stats collection.
--tablespaces  Use tablespaces.
-U  Run test as an unprivileged operating system and database user.  By
        default the test is run as a privileged user that can stop and start
        the database between the individual tests, as allowed per
        specification.
-V, --version  Output version information, then exit.
-Y  Enable profiling (Linux only).
--help  This usage message.  Or **-?**.

PostgreSQL options (pgsql):

-p PARAMETERS  Database *parameters*.

*dbms* options are:

* gp Greenplum
* pgsql PostgreSQL

ENVIRONMENT
===========

The following variables are required and are normally exported by
**dbt7-run**\ (1):

**DBT7DBNAME**

    Name of the database to load.

**DSHOME**

    Absolute path to the TPC-DS Tools directory.

**DSTOOLS**

    Path to the TPC-DS Tools *tools* subdirectory.

**DSDISTRIBUTIONS**

    Path to the TPC-DS Tools *tpcds.idx* distributions file.

**DS_DATA**

    Directory where the generated flat files are saved.

Optional:

**PERFCOMMAND**

    A simple command prefix for the load, e.g. "perf stat"; shell syntax
    such as pipes or redirection is not supported.

SEE ALSO
========

**dbt7**\ (1), **dbt7-run**\ (1), **dbt7-power-test**\ (1),
**dbt7-throughput-test**\ (1)
